<h1>All the Dogs</h1>

<%=form_tag(dogs_path, method: "get") do %>
    <%= hidden_field_tag(:sort, params[:sort]=true) %>
    <%= submit_tag("Sort by Name Alphabetically") %>
    <% end %>

<% @dogs.each do |dog| %>
    <h3><%=link_to dog.name, dog_path(dog)%></h3>
    <% end %>